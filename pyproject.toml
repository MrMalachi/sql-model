[project]
name = "sql-model"
version = "0.1.0"
description = "Project directory for sql model practice."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "sqlmodel>=0.0.39",
]

[project.scripts]
sqlmodel = "sqlmodel:main"
